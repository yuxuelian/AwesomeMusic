<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.kaibo.music">

    <application
        android:name="com.kaibo.music.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name="com.kaibo.music.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.kaibo.music.activity.SearchActivity" />
        <activity android:name="com.kaibo.music.activity.SongListActivity" />
        <activity android:name="com.kaibo.music.activity.PlayerActivity" />

        <service android:name=".play.PlayerService" />
    </application>

</manifest>